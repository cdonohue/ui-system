---
name: Typography
route: /typography
order: 1600
---

import { Playground } from "docz"
import {
  Box,
  Container,
  ConfigProvider,
  ConfigConsumer,
  Heading,
  Text,
  Paragraph,
  List,
  ListItem,
} from "../src"
import SectionHeading from "./components/SectionHeading"
import ConfigPropertyList from "./components/ConfigPropertyList"
import FontBox from "./components/FontBox"
import LetterBox from "./components/LetterBox"
import TypeUtilityBox from "./components/TypeUtilityBox"

# Typography

<SectionHeading pattern="font-{family}">Font Family</SectionHeading>

<ConfigConsumer>
  {({ config }) => (
    <Box
      style={{
        display: "grid",
        gridTemplateColumns: "repeat(auto-fill, minmax(320px, 1fr))",
        gridGap: "1rem",
      }}
    >
      {Object.keys(config.baseValues.fonts).map((fontClass) => (
        <FontBox
          caption={fontClass}
          modifier={`font-${fontClass}`}
          fonts={config.baseValues.fonts[fontClass]}
        />
      ))}
    </Box>
  )}
</ConfigConsumer>

<SectionHeading pattern="leading-{size}">Line Height</SectionHeading>

<ConfigConsumer>
  {({ config }) => (
    <Box
      style={{
        display: "grid",
        gridTemplateColumns: "repeat(auto-fill, minmax(320px, 1fr))",
        gridGap: "1rem",
      }}
    >
      {Object.keys(config.baseValues.lineHeights).map((size) => (
        <TypeUtilityBox
          caption={size}
          example={
            "Portland flannel trust fund plaid kitsch. Hot chicken everyday carry williamsburg YOLO green juice lomo. Bicycle rights man bun +1 shabby chic migas salvia listicle polaroid paleo."
          }
          modifier={`leading-${size}`}
          value={config.baseValues.lineHeights[size]}
        />
      ))}
    </Box>
  )}
</ConfigConsumer>

<SectionHeading pattern="tracking-{size}">Letter Spacing</SectionHeading>

<ConfigConsumer>
  {({ config }) => (
    <Box
      style={{
        display: "grid",
        gridTemplateColumns: "repeat(auto-fill, minmax(320px, 1fr))",
        gridGap: "1rem",
      }}
    >
      {Object.keys(config.baseValues.letterSpacing).map((size) => (
        <TypeUtilityBox
          caption={size}
          modifier={`tracking-${size}`}
          value={config.baseValues.letterSpacing[size]}
        />
      ))}
    </Box>
  )}
</ConfigConsumer>

<SectionHeading pattern="text-{size}">Font Sizes</SectionHeading>

<ConfigConsumer>
  {({ config }) => (
    <Box
      style={{
        display: "grid",
        gridTemplateColumns: "repeat(auto-fill, minmax(150px, 1fr))",
        gridGap: "1rem",
      }}
    >
      {Object.keys(config.baseValues.textSizes).map((size) => (
        <LetterBox
          caption={size}
          modifier={`text-${size}`}
          value={config.baseValues.textSizes[size]}
        />
      ))}
    </Box>
  )}
</ConfigConsumer>
