---
name: Flexbox
menu: Utilities
---

import { Box } from "../src"
import FlexBox from "./components/FlexBox"

# Flexbox

Use the `flex` and `inline-flex` classes to control the display of your component.

## Direction

<Box
  style={{
    display: "grid",
    gridTemplateColumns: "repeat(auto-fill, minmax(175px, 1fr))",
    gridGap: "1rem",
  }}
>
  <FlexBox even flexModifier="flex-row" />
  <FlexBox even flexModifier="flex-row-reverse" />
  <FlexBox even stretch flexModifier="flex-col" />
  <FlexBox even stretch flexModifier="flex-col-reverse" />
</Box>

## Align Items

<Box
  style={{
    display: "grid",
    gridTemplateColumns: "repeat(auto-fill, minmax(175px, 1fr))",
    gridGap: "1rem",
  }}
>
  <FlexBox flexModifier="items-start" />
  <FlexBox flexModifier="items-center" />
  <FlexBox flexModifier="items-end" />
  <FlexBox flexModifier="items-stretch" />
  <FlexBox flexModifier="items-baseline" />
</Box>

## Justify Content

<Box
  style={{
    display: "grid",
    gridTemplateColumns: "repeat(auto-fill, minmax(175px, 1fr))",
    gridGap: "1rem",
  }}
>
  <FlexBox even flexModifier="justify-start" />
  <FlexBox even flexModifier="justify-center" />
  <FlexBox even flexModifier="justify-end" />
  <FlexBox even flexModifier="justify-between" />
  <FlexBox even flexModifier="justify-around" />
</Box>
